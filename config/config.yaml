defaults:
  - _self_
  - dataset: mls_eng
  - dataloader: default
  - model: base
  - training: full_run
  - wandb: full_run
  # `setup:` loads after `training:` and `wandb:` so setup recipes (`# @package _global_`)
  # can override training fields like `lr_schedule` and wandb configs.
  - setup: full_run
