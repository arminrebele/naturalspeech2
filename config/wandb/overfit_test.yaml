defaults:
  - base
  - _self_

group: "overfit-test"
run_name: "overfit_test"
# Override on the CLI to give the run initial context, e.g.
#   wandb.notes='"first 2k run with latent normalization"' wandb.tags='[latent-norm,2k-iter]'
