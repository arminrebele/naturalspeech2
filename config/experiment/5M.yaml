# @package _global_

defaults:
  - override /setup: gradient_analysis

run_name: "5M"                     # one knob → wandb run + logs + checkpoints (a re-used name auto-bumps to 5M-2)

dataset:
  max_train_clips: 5000000

wandb:
  group: "gradient-analysis"
