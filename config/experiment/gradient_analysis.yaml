# @package _global_
#
# Full generalization run (eval + checkpoints + safetensors, like the main run) + per-loss-term gradient
# diagnostic — python scripts/train.py +experiment=gradient_analysis

defaults:
  - override /setup: gradient_analysis
  - override /wandb: gradient_analysis

dataset:
  max_train_clips: 5000000
