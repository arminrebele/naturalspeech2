services:
  ns2:
    build: .
    # Increase shared memory size for PyTorch DataLoader workers
    shm_size: '128g'
    volumes:
      - .:/workspace
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              capabilities: [gpu]
              count: all
    # This keeps the container's bash shell alive if started in the background
    tty: true
    stdin_open: true
